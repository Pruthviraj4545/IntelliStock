# PostgreSQL Configuration
DB_USER=postgres
DB_PASSWORD=your_secure_password_here
DB_HOST=localhost
DB_PORT=5432
DB_NAME=intellistock_db

# ML service authentication (required for /sales-data, /train-model, /forecast)
# Send as header: X-API-Key: <value>
ML_API_KEY=change-me
